// Legacy fixed fees (kept for reference, no longer used by default)
export const FEES = {
  digital: 1.95,
  magnet: 5.45,
  collector: 9.95,
  default: 2,
};

// Association receives 40% of the card price, platform keeps 60%
export const ASSOCIATION_SHARE_PERCENTAGE = 40;
export const PLATFORM_FEE_PERCENTAGE = 60;
